import ScrambleText from './ScrambleText.tsx';

export default function HeroOverlay({ progress }: { progress: number }) {
  const fadeOut = Math.max(0, Math.min(1, progress / 0.35));

  return (
    <div className="absolute inset-0 z-30 pointer-events-none" style={{ opacity: 1 - fadeOut }}>
      <div className="h-full flex flex-col justify-center ml-6 sm:ml-16 lg:ml-24 xl:ml-32">
        <div className="max-w-xl">
          <h1 className="text-5xl sm:text-6xl lg:text-7xl font-bold text-text leading-[1.1] tracking-tight">
            See your site
            <br />
            on every{' '}
            <span className="text-accent">screen.</span>
          </h1>
          <p className="mt-5 text-base sm:text-lg text-text-muted max-w-md leading-relaxed">
            Test your website on{' '}
            <ScrambleText />
          </p>
          <div className="mt-8 flex flex-col sm:flex-row items-start gap-3 sm:gap-4">
            <pre className="bg-surface border border-border rounded-xl px-5 py-3 text-sm text-text-dim font-mono select-all cursor-text pointer-events-auto">
              <code>npm i viewport</code>
            </pre>
            <a
              href="/app"
              className="inline-flex items-center gap-2 px-5 py-3 text-sm font-semibold text-white bg-accent rounded-xl hover:bg-accent-hover transition-colors shadow-lg shadow-accent/25 pointer-events-auto"
            >
              Try It Free
              <svg width="16" height="16" viewBox="0 0 256 256" fill="currentColor">
                <path d="M224.49,136.49l-72,72a12,12,0,0,1-17-17L187,140H40a12,12,0,0,1,0-24H187L135.51,64.48a12,12,0,0,1,17-17l72,72A12,12,0,0,1,224.49,136.49Z" />
              </svg>
            </a>
          </div>
          <p className="mt-6 text-xs text-text-dim">
            No account required. Completely free.
          </p>
        </div>
      </div>
    </div>
  );
}
